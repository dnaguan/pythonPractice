import time
import webbrowser

total_breaks = 3
break_count = 0

print("This program started on " + time.ctime())
while(brek_count < total_breaks):
    time.sleep(10)
    webbrowser.open("")
    brek_count = brek_count + 1
